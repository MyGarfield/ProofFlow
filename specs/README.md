# 设计规格

本目录仅保存 `DESIGN_ONLY` 的 ProofFlow 设计规格。

- `06_AGENT_IDENTITY.yaml`：六 Agent Identity 清单。
- `07_SKILL_SPEC.yaml`：八 Skill 规格清单。

两个文件均复制自本地 `work/package_materials/` 的已校验版本。复制不代表规格已经实现，所有条目的实现状态仍为 `NOT_IMPLEMENTED`。

若未来修改规格，计划同时更新版本号、变更说明和对应验证用例。当前未建立发布或兼容性承诺。
